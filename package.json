{
  "name": "log-solar-system",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Interactive 3D logarithmic-scale solar system demo (Vite + TypeScript + Three.js)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && npm run verify",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "verify": "node scripts/verify.mjs"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "esbuild": "^0.21.5",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
